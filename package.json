{
  "name": "grain6-get-started",
  "version": "1.0.0",
  "description": "Grain6 Get Started - Svelte website with grainpath subdirs",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.0.0",
    "svelte": "^4.2.0",
    "vite": "^5.0.0",
    "gh-pages": "^6.0.0"
  },
  "dependencies": {
    "svelte-routing": "^1.7.0"
  }
}

