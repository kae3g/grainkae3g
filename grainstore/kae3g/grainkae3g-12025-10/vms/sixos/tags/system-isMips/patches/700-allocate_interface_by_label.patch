diff --git a/drivers/staging/octeon/ethernet.c b/drivers/staging/octeon/ethernet.c
index 9eee28f2940c..81f8ec74a206 100644
--- a/drivers/staging/octeon/ethernet.c
+++ b/drivers/staging/octeon/ethernet.c
@@ -408,6 +408,10 @@ int cvm_oct_common_init(struct net_device *dev)
 {
 	struct octeon_ethernet *priv = netdev_priv(dev);
 	int ret;
+	const u8 *label = NULL;
+
+	if (priv->of_node)
+		label = of_get_property(priv->of_node, "label", NULL);
 
 	ret = of_get_ethdev_address(priv->of_node, dev);
 	if (ret)
@@ -441,6 +445,9 @@ int cvm_oct_common_init(struct net_device *dev)
 	if (dev->netdev_ops->ndo_stop)
 		dev->netdev_ops->ndo_stop(dev);
 
+	if (!IS_ERR_OR_NULL(label))
+		dev_alloc_name(dev, label);
+
 	return 0;
 }
 
